name: Site

# Separate from ci.yml on purpose (RFC 0003 §6, CLAUDE.md): the site/
# toolchain (Node) never touches the Python gates (lint/typecheck/test/
# derived-csv-in-sync/original-raw-immutable/...).
#
# `push` to main has NO path filter, deliberately: the emitter
# (scripts/emit_site_data.py) depends on the whole Python domain library, and
# the RFC 0003 §2/§7 freshness proof requires every push to main to rebuild
# and republish — a path filter here would let the deployed SHA silently lag
# behind main whenever a commit touched something outside the filtered
# paths. `pull_request` keeps a path filter — but it has to list every
# source the site actually publishes, or a PR changing published content
# merges without ever having been built. Since Fase C, `docs/**` is one of
# those sources (docs/analysis/ and docs/rfc/ are content collections), so
# a PR touching only a relatório now *does* need a site rebuild.
on:
  pull_request:
    paths:
      - "site/**"
      - "okf/**"
      - "docs/**"
      - "scripts/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/site.yml"
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: site-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck:
    # Mirrors ci.yml's own lint/typecheck/test split: a separate job/status
    # per concern, not one job silently doing everything. `astro check` IS
    # this project's "tsc --noEmit" — it type-checks every .ts file too
    # (verified: an intentional type error in a plain .ts file was caught),
    # plus .astro templates and the generated content-collection types that
    # a bare `tsc` can't parse on its own.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: "0.7.19"
          enable-cache: true
      - run: uv sync --dev
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Emit site data (dados-do-site.json) — RFC 0003 §4
        # Needed even just to type-check: content.config.ts's collections
        # (and lib/site-data.ts's JSON import) only resolve once this exists.
        run: ./site/scripts/emit-data.sh
      - name: Install site dependencies
        working-directory: site
        run: npm ci
      - name: Type-check (astro check)
        working-directory: site
        run: npm run check

  test:
    # The simulador's (RFC 0002) conservative exclusion filter — site/src/lib/
    # simulador.ts, parse-sisprev.ts — is plain TS with no Astro/content
    # dependency, so this job doesn't need the emitter or a content sync,
    # unlike typecheck above.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Install site dependencies
        working-directory: site
        run: npm ci
      - name: Run unit tests (vitest)
        working-directory: site
        run: npm run test

  build:
    # Só em push para main (decisão 2026-07-29). O build é o job caro do
    # repositório: instala o toolchain Node, roda o Astro, o Pagefind, o Pango
    # do sistema e pagina ~490 páginas de PDF com o WeasyPrint. Em PR ele
    # rodava e o artefato do Pages já era descartado, então o custo era pago
    # sem ninguém publicar nada.
    #
    # **O que se perde, declarado**: o `gerar_relatorio_pdf.py` era a única
    # coisa que provava, antes do merge, que a paginação ainda funciona — um
    # recurso que não resolve (`url_fetcher`) ou uma regra de Paged Media
    # quebrada só aparece agora **depois** do merge, no push para main, e o
    # sintoma é o job vermelho no main em vez de na PR. `typecheck` e `test`
    # continuam em PR, então erro de tipo, de content collection e das
    # bibliotecas puras (`painel`, `filtros`, `relatorio`, `simulador`) segue
    # barrado antes do merge; o que sai é a prova de paginação e a de que o
    # emissor consegue emitir.
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [typecheck, test]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        with:
          version: "0.7.19"
          enable-cache: true
      - run: uv sync --dev
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Emit site data (dados-do-site.json) — RFC 0003 §4
        run: ./site/scripts/emit-data.sh
      - name: Install site dependencies
        working-directory: site
        run: npm ci
      - name: Build site (astro build)
        working-directory: site
        run: npm run build
      - name: Install WeasyPrint's system libraries
        # WeasyPrint renderiza texto pelo Pango, que é biblioteca do sistema e
        # não vem pelo `uv sync`. Instalada explicitamente em vez de contar com
        # o que a imagem do runner traz hoje: o modo de falha é o PDF sair sem
        # tipografia certa (ou não sair), num artefato que ninguém reabre antes
        # de juntar ao processo.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b
      - name: Generate the PGE validation report PDF into dist/
        # Depois do `npm run build` (e do Pagefind, que roda no postbuild) e
        # antes do upload: o PDF é escrito em `site/dist/`, então ele sobe no
        # mesmo artefato do Pages e fica publicado ao lado da página que o
        # oferece. Um erro aqui (folha de estilo que não resolve, por
        # exemplo) é falha explícita, não PDF torto em silêncio — mas, desde
        # que o job passou a ser só de push, essa falha aparece depois do
        # merge e não na PR (ver o comentário do `if` do job).
        run: uv run python scripts/gerar_relatorio_pdf.py
      # Sem `if` nos dois passos abaixo: o job inteiro já é só de push para
      # main, então a condição que eles carregavam virou redundante.
      - name: Configure Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5.0.0
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4.0.0
        with:
          path: site/dist

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5
      - name: Smoke check — deployed page responds and matches this commit's SHA
        # RFC 0003 §7: a deploy that silently fails must not leave a stale
        # site serving an old commit's content unnoticed — this is the check
        # that makes that failure visible instead. Retries: Pages' CDN can
        # briefly keep serving the previous deploy right after this action
        # reports success, so a single immediate curl can false-negative.
        run: |
          set -euo pipefail
          url="${{ steps.deployment.outputs.page_url }}"
          sha="${{ github.sha }}"
          max_attempts=10
          delay_seconds=6
          for attempt in $(seq 1 "$max_attempts"); do
            if body="$(curl -fsSL "$url")" && grep -q "$sha" <<< "$body"; then
              echo "OK: $url serves commit $sha (attempt $attempt/$max_attempts)."
              exit 0
            fi
            echo "Attempt $attempt/$max_attempts: $url does not yet show commit $sha. Retrying in ${delay_seconds}s..."
            sleep "$delay_seconds"
          done
          echo "Deployed page at $url still does not show the expected commit SHA $sha after $max_attempts attempts."
          exit 1
