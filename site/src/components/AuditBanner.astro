---
// Global audit-state notice (RFC 0003 §5, obrigatório enquanto nenhuma
// regra estiver validada): every page, not just the home, carries this —
// "detecção ≠ conclusão" applied to the reader. Never omit or hide this
// component while the catalog has zero regras with status_auditoria
// "validada".
---

<div class="audit-banner" role="note">
  <p>
    <strong>Catálogo em auditoria.</strong> Nenhuma regra deste catálogo concluiu
    o ciclo de validação (PGE/Presidência). O conteúdo publicado aqui reflete o
    estado corrente da auditoria, não um veredito oficial — confira o selo de
    estado em cada ficha.
  </p>
</div>
